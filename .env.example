# Healthcare Provider Directory - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control

# ============================================================================
# Databricks Connection Settings
# ============================================================================

# Your Databricks workspace URL (without https://)
# Example: adb-1234567890123456.7.azuredatabricks.net
DATABRICKS_HOST=your-workspace.cloud.databricks.com

# Personal Access Token (generate from Databricks Settings → Developer → Access Tokens)
# Keep this secret! Use Databricks Secrets in production
DATABRICKS_TOKEN=dapi...your-token-here...

# SQL Warehouse HTTP Path (get from SQL Warehouse → Connection Details)
# Example: /sql/1.0/warehouses/abc123def456
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/xxxxx

# ============================================================================
# Unity Catalog Configuration
# ============================================================================

# Comma-separated list of catalogs to search
# Leave empty to search all accessible catalogs
DATABRICKS_CATALOGS=databricks_poc

# Comma-separated list of schemas to search
# Leave empty to search all schemas
DATABRICKS_SCHEMAS=default

# Table name pattern filter (comma-separated regex patterns)
# Only tables matching these patterns will be loaded
# Example: batch_process_output,csv_upload_details,provider_verification
DATABRICKS_TABLE_PATTERN=batch_process_output,csv_upload_details

# Cache duration in minutes (how long to cache table list)
# Increase if your tables don't change frequently
DATABRICKS_CACHE_DURATION=5

# ============================================================================
# Flask Application Settings
# ============================================================================

# Flask secret key for session management
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# MUST be changed in production!
FLASK_SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Enable/disable Databricks integration
ENABLE_DATABRICKS=true

# Flask environment (development or production)
FLASK_ENV=production

# ============================================================================
# Application Features (Optional)
# ============================================================================

# Upload folder for local file storage (if using file upload feature)
# UPLOAD_FOLDER=uploads

# Data storage folder for processed data
# DATA_STORAGE=data_storage

# ============================================================================
# Deployment Notes
# ============================================================================

# For Databricks Apps deployment:
# 1. Set these variables in the Databricks Apps UI (Environment Variables section)
# 2. Use Databricks Secrets for DATABRICKS_TOKEN and FLASK_SECRET_KEY
# 3. Ensure SQL Warehouse is running before deploying
# 4. Verify Unity Catalog permissions

# For local development:
# 1. Copy this file to .env
# 2. Fill in actual values
# 3. Run: flask run --host=0.0.0.0 --port=8080
